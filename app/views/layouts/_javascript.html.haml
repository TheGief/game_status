- if current_user
  :coffeescript
    $ ->
      GS.user.id = #{current_user.id}
      GS.user.email = '#{current_user.email}'

      GS.init()
      GS.user_init()
- else
  :coffeescript
    GS.init()